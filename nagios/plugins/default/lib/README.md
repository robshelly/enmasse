# Nagios check helpers

The files here are meant to support Nagios checks, be reusable and automatically
tested.

## Running tests

From the directory where this file is located, run:

```
python -m unittest discover
```
